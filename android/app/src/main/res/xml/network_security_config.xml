<?xml version="1.0" encoding="utf-8"?>
<!--
    Allow cleartext HTTP for LAN sync. The server is local-network only and
    never exposed to the internet, so cleartext is acceptable here.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
